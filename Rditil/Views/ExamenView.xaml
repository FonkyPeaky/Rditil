<Page x:Class="Rditil.Views.ExamenView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid>
        <!-- CARD -->
        <Border Margin="28"
                Padding="26"
                CornerRadius="22"
                Background="White"
                SnapsToDevicePixels="True">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="RDITIL"
                                   FontSize="30"
                                   FontWeight="Black"
                                   Foreground="#0A0A0A"/>
                        <TextBlock Text="Examen blanc ITIL"
                                   Margin="0,2,0,0"
                                   FontSize="13"
                                   Foreground="#6B7280"/>
                        <TextBlock Text="{Binding QuestionNumeroText}"
                                   Margin="0,10,0,0"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="#0A0A0A"/>
                    </StackPanel>

                    <Border Grid.Column="1"
                            Background="#F3F4F6"
                            CornerRadius="14"
                            Padding="14,10"
                            VerticalAlignment="Top">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⏱" FontSize="16" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding TempsRestantText}"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="#111827"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- spacer -->
                <Border Grid.Row="2" Height="12" Background="Transparent"/>

                <!-- ENONCE -->
                <TextBlock Grid.Row="3"
                           Text="{Binding Enonce}"
                           TextWrapping="Wrap"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="#111827"
                           Margin="0,0,0,16"/>

                <!-- REPONSES -->
                <ItemsControl Grid.Row="4" ItemsSource="{Binding ReponsesChoix}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,12"
                                    CornerRadius="14"
                                    Background="#13B6E0"
                                    Padding="14">
                                <DockPanel LastChildFill="True">
                                    <CheckBox IsChecked="{Binding IsSelected}"
                                              VerticalAlignment="Center"
                                              Margin="0,0,12,0"/>
                                    <TextBlock Text="{Binding Text}"
                                               FontSize="16"
                                               Foreground="Black"
                                               TextWrapping="Wrap"/>
                                </DockPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </Grid>
        </Border>
    </Grid>
</Page>
