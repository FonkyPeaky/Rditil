<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Fonts -->
    <FontFamily x:Key="F_Title">Segoe UI Semibold</FontFamily>
    <FontFamily x:Key="F_Body">Segoe UI</FontFamily>

    <!-- Styles de texte (garde les 2 clés pour compat) -->
    <Style x:Key="TitleTxt" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource F_Title}"/>
        <Setter Property="FontSize" Value="42"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
    </Style>

    <Style x:Key="TitleText" TargetType="TextBlock" BasedOn="{StaticResource TitleTxt}"/>

    <Style x:Key="BodyTxt" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource F_Body}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.SubText}"/>
    </Style>

</ResourceDictionary>
